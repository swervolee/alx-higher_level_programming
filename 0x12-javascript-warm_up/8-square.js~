#!/usr/bin/node
const num = Math.floor(process.argv[2])

if (isNaN(num)) {
    console.log('Missing size');
} else {
    for (const i = 0; i < num; i++) {
	for (const j = 0; j < num; j++) {
	    console.log('X');
	}
    }
}
