#!/usr/bin/python3
'prints the x-request-id of a header'
from import sys import argv
import urllib.request


url = argv[1]
with urllib.request(url) as response:
    print(response.headert['X-Request-Id'])
